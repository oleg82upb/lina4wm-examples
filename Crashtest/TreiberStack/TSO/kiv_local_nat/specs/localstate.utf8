data specification
	using natref, PC, PID
	Localstate :=mkls( . .pc : PC; . .pid : PID;
					. .head : ref;
					. .call : ref;
					. .val : ref;
					. .next : ref;
					. .v0 : ref;
					. .v1 : ref;
					. .v2 : ref;
					. .v3 : ref;
					. .v4 : nat;
					. .v5 : nat;
					. .v6 : nat;
					. .cmp : nat;
					. .retval_0 : ref;
					. .this : ref;
					. .v : nat;
);
	variables
		ls , ls' : Localstate;
end data specification
